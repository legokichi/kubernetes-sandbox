apiVersion: v1
kind: Service
metadata:
  name: {{ template "myproduct.fullname" . }}
  labels:
    app: {{ template "myproduct.name" . }}
    chart: {{ template "myproduct.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "myproduct.name" . }}
    release: {{ .Release.Name }}
